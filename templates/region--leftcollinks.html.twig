{%
  set classes = [
    'dcf-footer-group',
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes).setAttribute('id', 'dcf-footer-group-2') }}>
    {{ content }}
  </div>
{% endif %}
